import React from 'react';
import {ThemeProvider, ProgressBar} from '../index';

export default (
    <ThemeProvider theme={'light_theme'}>
        <div>
            <ProgressBar/>
        </div>
    </ThemeProvider>
)