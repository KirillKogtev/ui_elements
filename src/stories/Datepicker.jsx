import React from 'react';
import {ThemeProvider, Datepicker} from "../index";

export default (
    <ThemeProvider theme={'light_theme'}>
        <div>
            <Datepicker/>
        </div>
    </ThemeProvider>
)